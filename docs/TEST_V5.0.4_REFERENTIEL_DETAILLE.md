# Test V5.0.4 — Référentiel détaillé mariage

- Même référentiel chargé depuis `js/mariage-creations.js` dans le portail et MyBusiness.
- Le portail conserve les choix standards dans `prestations` et le texte libre dans `autrePrestation`.
- La transformation en mariage ajoute uniquement les choix cochés et le texte libre.
- L’assistant de création manuelle propose les mêmes cases à cocher et un champ Autre.
- Aucune création n’est ajoutée par défaut si aucun choix n’est effectué.
